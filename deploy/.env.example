# 服务器公网 IP，不包含协议或端口。
SERVER_HOST=127.0.0.1
CUSTOMER_PORT=5173
ADMIN_PORT=5174
# 后台导入任务默认开启，接口文档默认关闭。
JOBS_ENABLED=true
API_DOCS_ENABLED=false
# 分别生成不同的随机密码。
DATABASE_PASSWORD=
REDIS_PASSWORD=
# 至少 32 字节的随机密钥。
JWT_SECRET=
# Base64 编码的 32 字节密钥，有数据后必须保留原值。
ENCRYPTION_KEY=
